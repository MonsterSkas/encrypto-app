#include <Headers.h>

class Encrypt
{
public:

    Encrypt()
    {
        std::cout << "\nEnter name of the file to encrypt";
        std::cout << "\n\n> ";

        Input();
    }

    void Input()
    {
        string file;
        std::cin >> file;

        
    }
};